from time import sleep


def img_to_url(img_path):
    return "/path/to/image"


def generate_images(query):
    # TODO: AI Generation happens here

    sleep(2)
    
    images = ["/path/to/image", "/path/to/image", "/path/to/image", "/path/to/image"]

    

def get_images(query):
    return [img_to_url(i) for i in generate_images(query)]